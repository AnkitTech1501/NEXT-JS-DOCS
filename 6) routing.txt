next js mai routing bahut simple hota hai with the help of app, appfolder next js ko routing provide karta hai 

hum usko bol sakte hai app routing in next js 

page.tsx in app yeh default rendering page hai 

agar appko cutom route banana hai toh next js bolta haio 

folder/page.jsx or page.js or page.tsx follow karna hai folder name hi appka url hai aur page.jsx appka content display 

app/page.tsx == '/' (url)

app/about/page.tsx == "/about" (url)

app/emloyee/page.tsx == "/employee" (url)

Routing flow 

Folder name
   ↓
URL create
   ↓
page.js render


mujhe about page ke content do baar do baar dikh raha hai.
kyu ki maine children do do baar likha hu layout.tsx mai 


import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import UserProvider from "./providers/UserProvider";
import ListProvider from "./providers/ListProvider";

import { UserContext } from "./context/UserContext";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        
         <UserProvider>
          {children}
        </UserProvider>
        <ListProvider>
          {children}
        </ListProvider>
        </body>
    </html>
  );
}


yeh code hai aur children current page ko cal karta hai or content pass karne mai help karta hai component ke andar 

isme children, chldren, 2 time hai isliye 2 time render hoga content about url mai 


toh humne fix kiya hai 


import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import UserProvider from "./providers/UserProvider";
import ListProvider from "./providers/ListProvider";

import { UserContext } from "./context/UserContext";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Create Next App",
  description: "Generated by create next app",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">
        
         <UserProvider>
          <ListProvider>
          {children}
        </ListProvider>
        </UserProvider>
        
        </body>
    </html>
  );
}


idhar 1 time hai chikldren so content bhi 1 time aayega 

important know everuypone -- agar aap folder mai  capital folder mai likho then uska url nhi banega aapko 404 not found error comes aayega..

e.g : app\EMPLOYEE\page.tsx  aisa work nhi hoga kyuki EMPLOYEE capital hai Pascal case chalge even folder name first capital bhi chalega aur lower case bhi chalega 

naming conventio 

component -- Pascal case 

folder name - Pascal case and lower case 

function define  - lower case aur Pascal case 


******** NESTED ROUTING ********
app\employee\show\page.jsx 

url : \employee\show 

aur render ui code : page.jsx 


********* NAVIGATION ***********

Page change karne ke liye Link use karte hai.

import Link from "next/link";

export default function Home() {
  return (
    <>
      <Link href="/about">About</Link>
    </>
  );
}

Ye SPA navigation karta hai:

full page reload nahi
fast transition

******* DYNAMIC ROUTNG **********

app\product\[id]\page.jsx

id mai kum kuch bhi pass kar sakte hai..
isko dynamic routing bolte hai ...
kuch bhi change id daalo vah render ho jayega...

app\product\[id]\page.jsx 

export default function Product({ params }) {
   return <h1>Product ID: {params.id}</h1>;
}

yeh code daalmagar error dikha console mai 

page.jsx:2  Server  Error: Route "/product/[id]" used `params.id`. `params` is a Promise and must be unwrapped with `await` or `React.use()` before accessing its properties. Learn more: https://nextjs.org/docs/messages/sync-dynamic-apis
    at Product (page.jsx:2:35)

Aap Next.js ke newer version use kar rahe ho jaha params async ho gaya hai App Router me.
Isliye await use karna padega ✅

export default async function ProductPage({ params }) {
  const { id } = await params;

  return <h1>Product {id}</h1>
  ;
}

************* MULTIPLE DYNAMIC ROUTING *************
mtlab ek se jyada dynamci route aur ek se jyada changeable value in url 

folder Structure

app/
 └── blog/
      └── [category]/
           └── [slug]/
                └── page.jsx

e.g 
/blog/tech/react-hooks


page .jsx mai 

export default async function BlogPage({ params }) {

  const { category, slug } = await params;

  return (
    <>
      <h1>Category: {category}</h1>
      <h2>Slug: {slug}</h2>
    </>
  );
}

*********slug -- readable text url ************

agar n number of dynamc routing use kare iska matlab humhe nhi pata kitna dynamic routing baneag aur uske hisab se hum folder bhi nhi create kar sakrte then

app/docs/[...slug]/page.jsx


URL:

/docs/react/hooks/useState

[...slug] ko Catch-All Dynamic Route bolte hai.

Ye multiple URL parts ko ek array me capture karta hai.

URL : /docs/react
params.slug: ["react"]

URL : /docs/react/hooks/useState
params.slug: ["react", "hooks", "useState"]


page.jsx 

export default async function DocsPage({ params }) {

  const { slug } = await params;

  return (
    <>
      <h1>{JSON.stringify(slug)}</h1>
    </>
  );
}

Output

URL:

/docs/react/hooks/useEffect

Output:

["react","hooks","useEffect"]

********* JSON.stringify ***************

JSON.stringify use hota ai convert karta jhai object or arryay ko string mai 

yeh use d hota hai api bhejne mai raw data,localstorage me, debugging mai  broswer mai object value dikhane ko , copnsole mai 

react mai abh direct object render nhi karte kyuki error atta hai 

Objects are not valid as a React child

e.g 
const user = {
  name: "Ankit",
  age: 25
};

JSON.stringify(user)

Output:

'{"name":"Ankit","age":25}'

same as arr 

["name","hello"];

JSON.stringify(user)

Output:

["name","hello"]


**********JSON.parse *******

convert karta hai string into object or else an array 

e.g 
const data  = {key1 : 'hello', key2 : "world"};
const res = JSON.stringify(data);
console.log(res);

output : {"key1" ; "hello","key2" : "world"}


JSON.parse(res);

use karoge direct object banake dega 

agar array se string kiye JSON.stringify se so jab tumn JSON.parse karoge toh yeh string se array banake dega 

******************important remember ***********

jo bhi url mai parameter name pass kar rahe ho vahi paramter name tum code mai likho ge 

for e.g dynamic routing 

/category/[slug]/page.tsx --- folder name slug hai jo dynamc kuch bhi value le sakte jo url se aur usi slug ko aap code mai likho 
otherwise kuch data nhi dikhega jo url name nhi likhoga 

export default async function Category({ params }) {
    const { slug } = await params;
    return <><h1>{JSON.stringify(slug)}</h1></>;
}   

agar yahi slug ke alawa hum category daale magar data nhi dikega 

bilkul yeh object destructing ki tarah work karta hai 

*********************OBJECT DESTRUCTING *****************************
const user = {name : 'ankit',first:'dubey',std : '10'};
const {name,...expect} = user;  // agar name nhi likhoga toh error aayega kyuki name key hai same key aap destructing mai use kar sakte ho 

console.log(name);  // op : ankit
console.log(expect); // op : { age: 25, city: 'Mumbai' }

same as array destructing mai bhi bus aaray mai key nhi rahega 
const numbers = [10, 20, 30, 40, 50];

const [first, ...rest] = numbers;

console.log(first);
console.log(rest);

o/p :

10
[20, 30, 40, 50]

abh ... yeh rest operator hai rrest operator mtlab data collect karna 

... spread operator mntlab data failana 

... both spread and rest operator symbol but working differnt 

// pichle vaala rest operartor ka example tha abh spread operator ///


const arr = [1,2,4,5];
const arr1 = [...arr,45,55];
console.log(arr1);

o/p : [1,2,3,4,5,45,55];

aur yeh shallow copy bhio banata hai 

like 

const arr2 = [...arr];
o/p : [1,2,4,5]
agara arr2 ka data change karoge toh arr1 mai effect nhi padega

arr2[4] = 78

o/p : [1,2,4,5,78]

samea s objhect mai bhi 

const obj = {name : 'ankit',last:'dubey',std : '75'};
const obj1 = {...obj1,st:45};

console.log(obj1); 

// op : {name:'ankit',last:'dubey',std:'75',st:45}








